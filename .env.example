# Base URL of the zwilt-server API the admin app proxies to (/api/* and /graphql).
# Local dev: http://localhost:5005  (the next.config.js rewrite falls back to this if unset)
# Production (set in Vercel): https://api.zwilt.com
NEXT_PUBLIC_API_URL=
